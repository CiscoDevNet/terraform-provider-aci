---
# Documentation generated by "gen/generator.go"; DO NOT EDIT.
# In order to regenerate this file execute `go generate` from the repository root.
# More details can be found in the [README](https://github.com/CiscoDevNet/terraform-provider-aci/blob/master/README.md).
subcategory: "Application Management"
layout: "aci"
page_title: "ACI: aci_virtual_ip_address"
sidebar_current: "docs-aci-resource-aci_virtual_ip_address"
description: |-
  Manages ACI Virtual IP Address
---

# aci_virtual_ip_address #

Manages ACI Virtual IP Address



## API Information ##

* Class: [fvVip](https://pubhub.devnetcloud.com/media/model-doc-latest/docs/app/index.html#/objects/fvVip/overview)

* Supported in ACI versions: 1.2(1i) and later.

* Distinguished Name Format: `uni/tn-{name}/ap-{name}/epg-{name}/vip-[{addr}]`

## GUI Information ##

* Location: `Tenants -> Application Profiles -> Application EPGs -> L4-L7 Virtual IPs`

## Example Usage ##

The configuration snippet below creates a Virtual IP Address with only required attributes.

```hcl

resource "aci_virtual_ip_address" "example_application_epg" {
  parent_dn = aci_application_epg.example.id
  ip        = "1.1.1.4"
}

```
The configuration snippet below shows all possible attributes of the Virtual IP Address.

!> This example might not be valid configuration and is only used to show all possible attributes.

```hcl

resource "aci_virtual_ip_address" "full_example_application_epg" {
  parent_dn   = aci_application_epg.example.id
  ip          = "1.1.1.4"
  annotation  = "annotation"
  description = "description_1"
  name        = "name_1"
  name_alias  = "name_alias_1"
  owner_key   = "owner_key_1"
  owner_tag   = "owner_tag_1"
  annotations = [
    {
      key   = "key_0"
      value = "value_1"
    }
  ]
  tags = [
    {
      key   = "key_0"
      value = "value_1"
    }
  ]
}

```

All examples for the Virtual IP Address resource can be found in the [examples](https://github.com/CiscoDevNet/terraform-provider-aci/tree/master/examples/resources/aci_virtual_ip_address) folder.

## Schema ##

### Required ###

* `parent_dn` - (string) The distinguished name (DN) of the parent object, possible resources:
  - [aci_application_epg](https://registry.terraform.io/providers/CiscoDevNet/aci/latest/docs/resources/application_epg) ([fvAEPg](https://pubhub.devnetcloud.com/media/model-doc-latest/docs/app/index.html#/objects/fvAEPg/overview))
* `ip` (addr) - (string) The L4-L7 Virtual IP Address of the Virtual IP Address object.

### Read-Only ###

* `id` - (string) The distinguished name (DN) of the Virtual IP Address object.

### Optional ###

* `annotation` (annotation) - (string) The annotation of the Virtual IP Address object. This attribute is supported in ACI versions: 3.2(1l) and later.
  - Default: `"orchestrator:terraform"`
* `description` (descr) - (string) The description of the Virtual IP Address object.
* `name` (name) - (string) The name of the Virtual IP Address object.
* `name_alias` (nameAlias) - (string) The name alias of the Virtual IP Address object. This attribute is supported in ACI versions: 2.2(1k) and later.
* `owner_key` (ownerKey) - (string) The key for enabling clients to own their data for entity correlation.
* `owner_tag` (ownerTag) - (string) A tag for enabling clients to add their own data. For example, to indicate who created this object.
* `annotations` - (list) A list of Annotations (ACI object [tagAnnotation](https://pubhub.devnetcloud.com/media/model-doc-latest/docs/app/index.html#/objects/tagAnnotation/overview)). Annotations can also be configured using a separate [aci_annotation](https://registry.terraform.io/providers/CiscoDevNet/aci/latest/docs/resources/annotation) resource. This attribute is supported in ACI versions: 3.2(1l) and later.
  #### Required ####
  
    * `key` (key) - (string) The key used to uniquely identify this configuration object.
    * `value` (value) - (string) The value of the property.
* `tags` - (list) A list of Tags (ACI object [tagTag](https://pubhub.devnetcloud.com/media/model-doc-latest/docs/app/index.html#/objects/tagTag/overview)). Tags can also be configured using a separate [aci_tag](https://registry.terraform.io/providers/CiscoDevNet/aci/latest/docs/resources/tag) resource. This attribute is supported in ACI versions: 3.2(1l) and later.
  #### Required ####
  
    * `key` (key) - (string) The key used to uniquely identify this configuration object.
    * `value` (value) - (string) The value of the property.

## Importing

An existing Virtual IP Address can be [imported](https://www.terraform.io/docs/import/index.html) into this resource with its distinguished name (DN), via the following command:

```
terraform import aci_virtual_ip_address.example_application_epg uni/tn-{name}/ap-{name}/epg-{name}/vip-[{addr}]
```

Starting in Terraform version 1.5, an existing Virtual IP Address can be imported
using [import blocks](https://developer.hashicorp.com/terraform/language/import) via the following configuration:

```
import {
  id = "uni/tn-{name}/ap-{name}/epg-{name}/vip-[{addr}]"
  to = aci_virtual_ip_address.example_application_epg
}
```
