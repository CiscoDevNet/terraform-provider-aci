// Code generated by "gen/generator.go"; DO NOT EDIT.
// In order to regenerate this file execute `go generate` from the repository root.
// More details can be found in the [README](https://github.com/CiscoDevNet/terraform-provider-aci/blob/master/README.md).

package provider

import (
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
	"github.com/hashicorp/terraform-plugin-testing/knownvalue"
	"github.com/hashicorp/terraform-plugin-testing/querycheck"
	"github.com/hashicorp/terraform-plugin-testing/querycheck/queryfilter"
	"github.com/hashicorp/terraform-plugin-testing/tfjsonpath"
)

{{- if .parents}}
	{{- range .parents}}
func TestAccListResource{{$.resourceClassName}}{{if .class_name}}With{{capitalize .class_name}}{{end}}(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t, "{{.test_type}}", "{{$.class_version}}") },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			{
				{{- if $.children}}
				// This class should be tested with children, for now there seems to be an issue with testing framework that prevents larger configs
				// Change testConfig{{$.resourceClassName}}Min to testConfig{{$.resourceClassName}}{{if $.children}}Children{{ else}}Min{{ end}}
				{{- end}}
				Config: testConfig{{$.resourceClassName}}Min{{if .class_name}}DependencyWith{{capitalize .class_name}}{{end}} + testConfigDataSourceSystem,
				{{- if eq $.resourceClassName "L3extRsRedistributePol"}}
				// Added temporary because the l3out resource has not been migrated yet which causes a plan change after apply on l3out
				ExpectNonEmptyPlan: true,
				{{- end}}
			},
			{
				Config: `
                list "aci_{{$.resourceName}}" "test" {
				  provider = aci
				  include_resource = true
                }
                `,
				Query: true,
				QueryResultChecks: []querycheck.QueryResultCheck{
					querycheck.ExpectLengthAtLeast("aci_{{$.resourceName}}.test", 1),
					querycheck.ExpectResourceKnownValues(
						"aci_{{$.resourceName}}.test",
						queryfilter.ByResourceIdentity(map[string]knownvalue.Check{"id": knownvalue.StringExact(fmt.Sprintf("%s%s",GetParentDnForTesting("{{.class_name}}", "{{$.pkgName}}"), "{{getRnForListTesting .target_classes $}}"))}),
						[]querycheck.KnownValueCheck{
							querycheck.KnownValueCheck{Path: tfjsonpath.New("id"), KnownValue: knownvalue.StringExact(fmt.Sprintf("%s%s",GetParentDnForTesting("{{.class_name}}", "{{$.pkgName}}"), "{{getRnForListTesting .target_classes $}}"))},
						},
					),
				},
			},
			{
				Config: fmt.Sprintf(`
                list "aci_{{$.resourceName}}" "test" {
				  provider = aci
				  include_resource = true
                  config {
					filter ="query-target-filter=eq({{$.pkgName}}.dn,\"%s\")"
				  }
                }
                `, fmt.Sprintf("%s%s", GetParentDnForTesting("{{.class_name}}", "{{$.pkgName}}"), "{{getRnForListTesting .target_classes $}}")),
				Query: true,
				QueryResultChecks: []querycheck.QueryResultCheck{
					querycheck.ExpectLength("aci_{{$.resourceName}}.test", 1),
					querycheck.ExpectResourceKnownValues(
						"aci_{{$.resourceName}}.test",
						queryfilter.ByResourceIdentity(map[string]knownvalue.Check{"id": knownvalue.StringExact(fmt.Sprintf("%s%s",GetParentDnForTesting("{{.class_name}}", "{{$.pkgName}}"), "{{getRnForListTesting .target_classes $}}"))}),
						[]querycheck.KnownValueCheck{
							querycheck.KnownValueCheck{Path: tfjsonpath.New("id"), KnownValue: knownvalue.StringExact(fmt.Sprintf("%s%s",GetParentDnForTesting("{{.class_name}}", "{{$.pkgName}}"), "{{getRnForListTesting .target_classes $}}"))},
						},
					),
				},
			},
		},
	})
}
	{{ end}}
{{- else}}
func TestAccListResource{{$.resourceClassName}}(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t, "{{.test_type}}", "{{$.class_version}}") },
		ProtoV6ProviderFactories: testAccProtoV6ProviderFactories,
		Steps: []resource.TestStep{
			{
				Config: testConfig{{$.resourceClassName}}{{if $.children}}Children{{ else}}Min{{ end}} + testConfigDataSourceSystem,
			},
			{
				Config: `
                list "aci_{{$.resourceName}}" "test" {
				  provider = aci
				  include_resource = true
                }
                `,
				Query: true,
				QueryResultChecks: []querycheck.QueryResultCheck{
					querycheck.ExpectLengthAtLeast("aci_{{$.resourceName}}.test", 1),
					querycheck.ExpectResourceKnownValues(
						"aci_{{$.resourceName}}.test",
						queryfilter.ByResourceIdentity(map[string]knownvalue.Check{"id": knownvalue.StringExact("uni/{{getRnForListTesting nil $}}")}),
						[]querycheck.KnownValueCheck{
							querycheck.KnownValueCheck{Path: tfjsonpath.New("id"), KnownValue: knownvalue.StringExact("uni/{{getRnForListTesting nil $}}")},
						},
					),
				},
			},
			{
				Config: `
                list "aci_{{$.resourceName}}" "test" {
				  provider = aci
				  include_resource = true
                  config {
					filter ="query-target-filter=eq({{$.pkgName}}.dn,\"uni/{{getRnForListTesting nil $}}\")"
				  }
                }
                `,
				Query: true,
				QueryResultChecks: []querycheck.QueryResultCheck{
					querycheck.ExpectLength("aci_{{$.resourceName}}.test", 1),
					querycheck.ExpectResourceKnownValues(
						"aci_{{$.resourceName}}.test",
						queryfilter.ByResourceIdentity(map[string]knownvalue.Check{"id": knownvalue.StringExact("uni/{{getRnForListTesting nil $}}")}),
						[]querycheck.KnownValueCheck{
							querycheck.KnownValueCheck{Path: tfjsonpath.New("id"), KnownValue: knownvalue.StringExact("uni/{{getRnForListTesting nil $}}")},
						},
					),
				},
			},
		},
	})
}
{{ end}}
