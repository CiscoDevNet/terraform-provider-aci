---
# Documentation generated by "gen/generator.go"; DO NOT EDIT.
# In order to regenerate this file execute `go generate` from the repository root.
# More details can be found in the [README](https://github.com/CiscoDevNet/terraform-provider-aci/blob/master/README.md).
subcategory: "{{ .SubCategory}}"
layout: "aci"
page_title: "ACI: aci_{{ .ResourceName}}"
sidebar_current: "docs-aci-list-resource-aci_{{ .ResourceName}}"
description: |-
  Manages ACI {{.ResourceNameAsDescription}}
---

# aci_{{ .ResourceName}} #

List existing ACI {{.ResourceNameAsDescription}} objects with the [terraform query](https://developer.hashicorp.com/terraform/cli/commands/query) command.

For more information about the usage see the official Hashicorp documentation for [importing existing resources in bulk](https://developer.hashicorp.com/terraform/language/v1.14.x/import/bulk?page=import&page=bulk).

## API Information ##

* Class: {{getDevnetDocForClass .PkgName}}
{{if ne .Versions ""}}
* Supported in ACI versions: {{ .Versions}}
{{- end}}

{{$length := len .DocumentationDnFormats}}{{if eq $length 1}}* Distinguished Name Format: `{{index .DocumentationDnFormats 0}}`{{else}}* Distinguished Name Formats:
    {{- range .DocumentationDnFormats}}
        {{- if containsString . "Too many"}}
  - {{ . }}
        {{- else}}
  - `{{ . }}`
        {{- end}}
    {{- end}}
{{- end}}

## GUI Information ##

{{$length := len .UiLocations}}{{if eq $length 1}}* Location: `{{ index .UiLocations 0}}`{{else}}* Locations:
    {{- range .UiLocations}}
  - `{{ . }}`
    {{- end}}
{{- end}}

## Example Usage ##

The configuration snippet below shows an example for {{.ResourceNameAsDescription}} with only required attributes.

```hcl
list "aci_{{ .ResourceName}}" "example" {
  provider = aci
}
```

The configuration snippet below shows an example for {{.ResourceNameAsDescription}} with filter capabilties.

```hcl
list "aci_{{ .ResourceName}}" "example" {
  provider = aci
  config {
    filter = "query-target-filter=eq({{.PkgName}}.dn,\"{{ index .DnFormats 0 }}\")"
  }
}
```

## Schema ##

### Optional ###

* `config` - (map) A map that contains all configurable options for the list resource.
    
    ### Optional ###

    * `filter` - (string) A filter to restrict the results of the class query to APIC. See [Cisco APIC REST API Configuration Guide, Release 4.2(x) and Later](https://www.cisco.com/c/en/us/td/docs/dcn/aci/apic/all/apic-rest-api-configuration-guide/cisco-apic-rest-api-configuration-guide-42x-and-later/m_using_the_rest_api.html#concept_1B75A78853DD46AABC15B048AAFAD1AD) for more information about composing filter query expressions.
