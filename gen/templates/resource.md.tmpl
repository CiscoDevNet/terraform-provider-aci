---
# Documentation generated by "gen/generator.go"; DO NOT EDIT.
subcategory: "{{ .SubCategory}}"
layout: "aci"
page_title: "ACI: aci_{{ .ResourceName}}"
sidebar_current: "docs-aci-resource-aci_{{ .ResourceName}}"
description: |-
  Manages ACI {{getResourceNameAsDescription .ResourceName}}
---

# aci_{{ .ResourceName}} #

Manages ACI {{getResourceNameAsDescription .ResourceName}}

## API Information ##

* `Class` - {{getDevnetDocForClass .PkgName}}

* `Supported ACI Versions` - `{{ .Versions}}`

* `Distinguished Name Formats`
    {{- range .DocumentationDnFormats}}
  - `{{ . }}`
    {{- end}}

## GUI Information ##

* `Location` - `{{ .UiLocation}}`

## Example Usage ##

The configuration snippet below creates a {{getResourceNameAsDescription .ResourceName}} with only required attributes.

```hcl
{{ .ExampleResource }}
```

{{- if not .HasOnlyRequiredProperties}}
The configuration snippet below shows all possible attributes of the {{getResourceNameAsDescription .ResourceName}}.

!> This example might not be valid configuration and is only used to show all possible attributes.

```hcl
{{ .ExampleResourceFull }}
```
{{- end}}

All examples for the {{getResourceNameAsDescription .ResourceName}} resource can be found in the [examples](https://github.com/CiscoDevNet/terraform-provider-aci/examples/resources/aci_{{.ResourceName}}) folder.

## Schema

### Required
{{ if .HasParent}}
* `parent_dn` - (string) The distinguished name (DN) of the parent object, possible resources:
  {{- range .DocumentationParentDns}}
  - {{ . }}
  {{- end}}
{{- end}}
{{- range .Properties}}
    {{- if or .IsNaming .IsRequired}}
* `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (string) {{.Comment}}
  - Supported ACI Versions: `{{ .Versions}}`
      {{- if .ValidValues }}{{$i := 1}}{{$length := len .ValidValues}}
  - Valid Values:{{ range .ValidValues}} `{{ . }}`{{ if ne $length $i}}{{$i = add $i 1}}, {{- else}}.{{- end}}{{- end}}
      {{- end}}
    {{- end}}
{{- end}}

### Read-Only

* `id` - (string) The distinguished name (DN) of the {{getResourceNameAsDescription .ResourceName}} object.
{{- if .HasOptionalProperties}}

### Optional
  {{ range .Properties}}
    {{- if and (not .IsNaming) (not .IsRequired)}}
      {{- if eq .ValueType "bitmask"}}
* `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}} ({{- .PropertyName}}) - (list) {{.Comment}}
  - Supported ACI Versions: `{{ .Versions}}`
        {{- if .DefaultValue }}
  - Default: `{{ .DefaultValue }}`.
        {{- end}}
      {{- else}}
* `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (string) {{.Comment}}
  - Supported ACI Versions: `{{ .Versions}}`
        {{- if .DefaultValue }}
  - Default: `{{ .DefaultValue }}`
        {{- end}}
        {{- if .ValidValues }}{{$i := 1}}{{$length := len .ValidValues}}
  - Valid Values:{{ range .ValidValues}} `{{ . }}`{{ if ne $length $i}}{{$i = add $i 1}}, {{- else}}.{{- end}}{{- end}}
        {{- end}}
      {{- end}}
    {{- end}}
  {{- end}}
{{- end}}
{{ range .Children }}
    {{- if ne .RelationshipClass ""}}
* `{{- .ResourceName}}` - (list) A list of {{getResourceNameAsDescription .ResourceName}} objects ({{getDevnetDocForClass .PkgName}}) pointing to the {{getResourceNameAsDescription .RelationshipResourceName}} ({{getDevnetDocForClass .RelationshipClass}}) which can be configured using the [aci_{{.RelationshipResourceName}}](https://registry.terraform.io/providers/CiscoDevNet/aci/latest/docs/resources/{{.RelationshipResourceName}}) resource.
  - Supported ACI Versions: `{{ .Versions}}`
    {{- else}}
* `{{- .ResourceName}}` - (list) A list of {{getResourceNameAsDescription .ResourceName}} objects ({{getDevnetDocForClass .PkgName}}) which can be configured using the [aci_{{.ResourceNameDocReference}}](https://registry.terraform.io/providers/CiscoDevNet/aci/latest/docs/resources/{{.ResourceNameDocReference}}) resource.
  - Supported ACI Versions: `{{ .Versions}}`
    {{- end}}
  
  #### Required
  {{ range .Properties}}
      {{- if or .IsNaming .IsRequired}}
          {{- if eq .ValueType "bitmask"}}
  * `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (list) {{.Comment}}
      - Supported ACI Versions: `{{ .Versions}}`
          {{- else}}
  * `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (string) {{.Comment}}
      - Supported ACI Versions: `{{ .Versions}}`
          {{- end}}
      {{- end}}
  {{- end}}
  {{- if .HasOptionalProperties}}

  #### Optional
    {{ range .Properties}}
      {{- if and (not .IsNaming) (not .IsRequired)}}
        {{- if eq .ValueType "bitmask"}}
  * `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (list) {{.Comment}}
      - Supported ACI Versions: `{{ .Versions}}`
        {{- else}}
  * `{{- overwriteProperty .PkgName .SnakeCaseName $.Definitions}}` ({{- .PropertyName}}) - (string) {{.Comment}}
      - Supported ACI Versions: `{{ .Versions}}`
        {{- end}}
        {{- if .DefaultValue }}
      - Default: `{{ .DefaultValue }}`
        {{- end}}
        {{- if .ValidValues }}{{$i := 1}}{{$length := len .ValidValues}}
      - Valid Values:{{ range .ValidValues}} `{{ . }}`{{ if ne $length $i}}{{$i = add $i 1}}, {{- else}}.{{- end}}{{- end}}
        {{- end}}
      {{- end}}
    {{- end}}
  {{- end}}
{{ end}}
## Importing

An existing {{getResourceNameAsDescription .ResourceName}} can be [imported](https://www.terraform.io/docs/import/index.html) into this resource via its distinguished name (DN), via the following command:

```
terraform import aci_{{- .ResourceName}}.example{{- if .DocumentationExamples}}_{{- $key :=  index .DocumentationExamples 0 }}{{getResourceName $key $.Definitions}}{{- end}} {{ index .DnFormats 0 }}
```

Starting in Terraform version 1.5, an existing {{getResourceNameAsDescription .ResourceName}} can be imported 
using [import blocks](https://developer.hashicorp.com/terraform/language/import) via the following configuration:

```
import {
  id = "{{ index .DnFormats 0 }}"
  to = aci_{{ .ResourceName}}.example{{- if .DocumentationExamples}}_{{- $key :=  index .DocumentationExamples 0 }}{{getResourceName $key $.Definitions}}{{- end}}
}
```
{{ if .DocumentationChildren}}
## Child Resources
  {{range .DocumentationChildren}}
  - {{ . }}
  {{- end}}
{{- end}}