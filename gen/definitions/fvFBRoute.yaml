documentation:
  resource:
    notes:
    - This resource should not be used in combination with the `vrf_fallback_routes`
      nested attribute of the `aci_vrf_fallback_route_group` resource. Doing so will
      result in unexpected behaviour.
  sub_category: Networking
  ui_locations:
  - Tenants -> Networking -> VRFs -> Policy -> Fallback Route Group -> Fallback Routes
is_single_nested_when_defined_as_child: true
properties:
  fbrPrefix:
    attribute_name: prefix_address
    default_values:
      2.2.2.2/24: ""
    documentation:
      description: The prefix address of the %s object.
    test_config:
      update:
      - config_value: 2001:0db8::01/64
resource_name: vrf_fallback_route
