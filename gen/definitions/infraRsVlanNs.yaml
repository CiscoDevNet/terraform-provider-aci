documentation:
  ui_locations:
  - Virtual Networking -> Provider -> Domain -> VLAN Pool
properties:
  tDn:
    documentation:
      description: The distinguished name of the target.
resource_name: relation_to_vlan_pool
test_config:
  dependencies:
  - class_name: fvnsVlanInstP
    config_overrides:
      alloc_mode: dynamic
      name: vlan_pool_name_1
    reference: aci_vlan_pool.test_vlan_pool_1.id
    role: target
