artifacts:
- resource
- datasource
documentation:
  sub_category: Application Management
  ui_locations:
  - Tenants -> Application Profiles -> uSeg EPGs -> uSeg Attributes
properties:
  match:
    default_values:
      any: ""
    documentation:
      description: The Matching Rule Type of the %s object.
    test_config:
      create:
      - config_value: all
  name:
    test_config:
      create:
      - config_value: criterion
  prec:
    attribute_name: precedence
    default_values:
      "0": ""
    documentation:
      description: The precedence of the %s object.
    test_config:
      create:
      - config_value: "1"
  scope:
    default_values:
      scope-bd: ""
    documentation:
      description: The scope of the %s object.
resource_name: epg_useg_block_statement
